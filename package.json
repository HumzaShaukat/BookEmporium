{
  "name": "googlebooks-app",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "engines": {
    "node": "16.18.1"
  },
  "scripts": {
    "start": "node server/server.js && cd ./bookemporium && npm start",
    "develop": "concurrently \"cd server && npm run watch\" \"cd bookemporium && npm start\"",
    "install": "cd server && npm i && cd ../bookemporium && npm i",
    "build": "cd bookemporium && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
